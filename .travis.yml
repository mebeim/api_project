os: linux
dist: trusty
sudo: required
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
language: c
git:
  depth: 3
compiler: gcc
install: false
script:
  - mkdir build && cd build
  - cmake ..
  - make simplefs_test
notifications:
  email: false