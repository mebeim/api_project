os: linux
dist: trusty
sudo: false
language: c
git:
  depth: 3
compiler:
  - gcc
install: true
script:
  - mkdir build && cd build
  - cmake ..
  - make
  - cd ../test
  - ./test_all.sh
notifications:
  email: false
  webhooks:
    urls:
      - https://fathomless-fjord-24024.herokuapp.com/notify
    on_success: change
    on_failure: always
    on_cancel: never
    on_error: always
